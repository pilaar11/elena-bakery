# ============================================================
# Elena Bakery — variables de entorno (PLANTILLA)
# Copia este archivo a ".env" y completa los valores.
# El archivo ".env" está en .gitignore y NO se sube al repo.
# ============================================================

# --- Públicas (seguras de exponer en el frontend) ---
# La anon/publishable key es pública por diseño: el acceso real
# a los datos lo controla Row Level Security (RLS) en Supabase.
SUPABASE_URL=https://evmgcrkxjjcjfjapepcx.supabase.co
SUPABASE_ANON_KEY=sb_publishable_PJGT2uyUW22AK8bNJemzQg_l5sg_9kt

# --- SECRETA (NUNCA en el frontend ni en el repo) ---
# Solo para herramientas locales: migraciones, seeds, scripts admin.
# Obtenla en: Supabase Dashboard → Project Settings → API → service_role.
SUPABASE_SERVICE_ROLE_KEY=
